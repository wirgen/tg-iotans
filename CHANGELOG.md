# Changelog

## 1.0.1

- Move `get_data` function from `__main__.py` to new `core.py` file
- Update import statements and file references
- Refactor `get_data` function signature to accept api_id, api_hash, and session as parameters
- Improve error handling in `get_version` function
- Update `__all__` list to include `get_data` function

## 1.0.0

- Initial release